// Shrothand Notation
const balance = 500;
if(balance >400) console.log("Test Completed"),console.log("Test complete2"); // Implicite scope // we can write 2 lines using coma(,) & we must need to add semi-Colon(;) after the implicite scope to end the scope otherwise it will show error

const userLoggedIn = true;
const debitCard = true;

if(userLoggedIn && debitCard){
    console.log("Allow to buy courses");
}

